# Cloudflare Pages — Redirects
# Doku: https://developers.cloudflare.com/pages/configuration/redirects/

# Apex → www wird per Cloudflare-Page-Rule (oder DNS-Redirect bei netcup)
# erledigt — Pages-_redirects akzeptiert nur relative Source-Pfade auf der
# eigenen Domain, kann also kein Cross-Hostname-Redirect.
#
# Alte Pfade / Legacy-Links — wenn später nötig hier ergänzen:
# /alt-pfad  /neuer-pfad  301
